#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    string s;
    cin>>s;
    int flag=0;
    for(int i=0; i<s.size(); i++)
    {
        if(islower(s[0]))
        {
            flag=1;
        }
    }
}